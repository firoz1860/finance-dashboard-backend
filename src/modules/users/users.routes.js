import { Router } from 'express';import { authenticate } from '../../middleware/auth.js';import { roleGuard } from '../../middleware/roleGuard.js';import { create, deactivate, getById, list, update } from './users.controller.js';const r=Router();r.use(authenticate,roleGuard(['ADMIN']));r.get('/',list);r.get('/:id',getById);r.post('/',create);r.patch('/:id',update);r.delete('/:id',deactivate);export default r;