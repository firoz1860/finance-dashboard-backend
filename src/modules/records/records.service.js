import prisma from '../../config/db.js';const where=(q)=>{const w={isDeleted:false};if(q.type)w.type=q.type;if(q.category)w.category={contains:q.category,mode:'insensitive'};if(q.search)w.OR=[{category:{contains:q.search,mode:'insensitive'}},{notes:{contains:q.search,mode:'insensitive'}}];if(q.startDate||q.endDate){w.date={};if(q.startDate)w.date.gte=new Date(q.startDate);if(q.endDate)w.date.lte=new Date(q.endDate);}return w;};export const listRecords=({query,skip,limit})=>prisma.financialRecord.findMany({where:where(query),include:{createdBy:{select:{id:true,name:true,email:true}}},orderBy:{date:'desc'},skip,take:limit});export const countRecords=(query)=>prisma.financialRecord.count({where:where(query)});export const getRecordById=(id)=>prisma.financialRecord.findFirst({where:{id,isDeleted:false},include:{createdBy:{select:{id:true,name:true,email:true}}}});export const createRecord=(payload,userId)=>prisma.financialRecord.create({data:{...payload,createdById:userId}});export const updateRecord=(id,payload)=>prisma.financialRecord.update({where:{id},data:payload});export const softDeleteRecord=(id)=>prisma.financialRecord.update({where:{id},data:{isDeleted:true}});