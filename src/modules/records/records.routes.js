import { Router } from 'express';import { authenticate } from '../../middleware/auth.js';import { roleGuard } from '../../middleware/roleGuard.js';import { create, getById, list, remove, update } from './records.controller.js';const r=Router();r.use(authenticate);r.get('/',list);r.get('/:id',getById);r.post('/',roleGuard(['ADMIN','ANALYST']),create);r.patch('/:id',roleGuard(['ADMIN']),update);r.delete('/:id',roleGuard(['ADMIN']),remove);export default r;